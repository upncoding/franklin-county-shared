export interface PDFPlaceholderProps {
    title: string;
    filename: string;
    description?: string;
    url?: string;
}
export declare function PDFPlaceholder({ title, filename, description, url }: PDFPlaceholderProps): import("react").JSX.Element;
